#ifndef TYPES_H
#define TYPES_H

#include <stdint.h>

// Définition des types standards pour l'émulation
typedef uint8_t u8;   // Un octet (0 à 255)
typedef uint16_t u16; // Deux octets (0 à 65535) - pour les adresses
typedef int8_t s8;    // Signé pour certains calculs

#endif